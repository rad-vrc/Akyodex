# Cloudflare Workers vs Pages 移行調査レポート

## 📋 エグゼクティブサマリー

### 調査結果の結論
**現時点での推奨**: **Cloudflare Pages での継続使用を推奨**

理由:
1. @opennextjs/cloudflare は Pages に最適化されている
2. 現在の設定で既に適切に動作している
3. Workers への移行にメリットが不明確
4. 移行コストとリスクが利益を上回る

### 70-80% Workers 使用率について
調査の結果、以下の可能性が考えられます:
- **誤解の可能性**: Pages も内部的には Workers を使用している
- **異なるユースケース**: カスタム API や低レベル制御が必要な場合
- **歴史的理由**: Pages の Next.js サポートは比較的新しい（2023年以降）

## ✅ 現在の実施ステータス（2026-02-16 20:17:05 JST / 2026-02-16 11:17:05 UTC）

| 項目 | 状態 | 備考 |
|------|------|------|
| Workers vs Pages の調査・比較 | ✅ 完了 | 本ドキュメントに比較結果と移行判断を記録済み |
| 方針決定（Pages 継続） | ✅ 完了 | 「移行不要」を採用 |
| Pages デプロイ互換性の修正 | ✅ 完了 | `fix: restore Pages deploy compatibility and add preview gate (#268)` |
| Cloudflare Pages Preview Gate 導入 | ✅ 完了 | PR ごとのデプロイ成否ゲートを運用中 |
| `/zukan` の CSP nonce 不一致対応 | ✅ 完了 | `fix(zukan): switch to dynamic rendering to resolve CSP nonce mismatch (#271)` |
| Workers 本番移行（Phase 1-4） | ⏳ 未着手 | 条件成立時のみ実施（本ドキュメントの長期方針） |

### 直近の結論
- 現在は **Cloudflare Pages 継続運用** が最適。
- Workers 移行は「必要条件が揃った場合のみ」実施。

---

## 🔍 詳細調査結果

### 1. Cloudflare Pages と Workers の違い

#### Cloudflare Pages
```
┌─────────────────────────────────────────────────────┐
│              Cloudflare Pages                        │
├─────────────────────────────────────────────────────┤
│ 目的: 静的サイト + フルスタックアプリケーション      │
│ 特徴:                                               │
│  ✅ Git 統合（自動デプロイ）                         │
│  ✅ プレビューデプロイ（PR ごと）                    │
│  ✅ 静的アセット最適化                               │
│  ✅ Functions（自動的に Workers として実行）         │
│  ✅ ビルドシステム統合                               │
│  ✅ 環境変数の UI 管理                               │
│  ✅ カスタムドメイン自動 SSL                         │
├─────────────────────────────────────────────────────┤
│ Next.js サポート:                                    │
│  • @cloudflare/next-on-pages（推奨）                │
│  • @opennextjs/cloudflare（このプロジェクトで使用） │
│  • ビルド出力: .vercel/output または .open-next     │
├─────────────────────────────────────────────────────┤
│ 制限:                                               │
│  ⚠️  Functions は 25MB まで                         │
│  ⚠️  ビルド時間 20分まで                            │
│  ⚠️  月間ビルド回数 500回（無料枠）                  │
└─────────────────────────────────────────────────────┘
```

#### Cloudflare Workers
```
┌─────────────────────────────────────────────────────┐
│            Cloudflare Workers                        │
├─────────────────────────────────────────────────────┤
│ 目的: サーバーレス関数・API・低レベル制御            │
│ 特徴:                                               │
│  ✅ 完全なコード制御                                 │
│  ✅ カスタムビルドパイプライン                       │
│  ✅ より大きなスクリプトサイズ（3MB → 10MB）         │
│  ✅ Durable Objects サポート                         │
│  ✅ Cron Triggers                                    │
│  ✅ WebSockets（より柔軟）                          │
├─────────────────────────────────────────────────────┤
│ Next.js サポート:                                    │
│  • 手動デプロイが必要                               │
│  • wrangler deploy コマンド                         │
│  • ビルド出力を Workers 形式に変換                  │
├─────────────────────────────────────────────────────┤
│ 欠点:                                               │
│  ❌ Git 統合なし（手動デプロイ）                     │
│  ❌ プレビューデプロイなし                           │
│  ❌ ビルドシステム非統合                             │
│  ❌ 環境変数は wrangler.toml または CLI で管理       │
└─────────────────────────────────────────────────────┘
```

### 2. 技術的な比較

| 項目 | Cloudflare Pages | Cloudflare Workers |
|------|-----------------|-------------------|
| **デプロイ方法** | Git push で自動 | `wrangler deploy` で手動 |
| **プレビュー** | PR ごとに自動 | なし（手動で設定必要） |
| **ビルド** | Cloudflare 側で実行 | ローカルまたは CI で実行 |
| **静的アセット** | 自動最適化 | 手動設定 |
| **環境変数** | Dashboard UI | wrangler.toml / CLI |
| **バインディング** | Dashboard UI | wrangler.toml |
| **スクリプトサイズ** | 25MB | 10MB (ただし Workers Assets で拡張可能) |
| **無料枠** | 500 ビルド/月 | 100,000 リクエスト/日 |
| **Next.js サポート** | 公式サポート | コミュニティサポート |

### 3. @opennextjs/cloudflare の設計

現在のプロジェクトが使用している `@opennextjs/cloudflare` は、**Cloudflare Pages に最適化**されています:

```typescript
// open-next.config.ts の分析
const config: OpenNextConfig = {
  buildCommand: 'npm run next:build',
  default: {
    override: {
      wrapper: 'cloudflare-node',      // Pages Functions 用
      converter: 'edge',                // Edge Runtime
      proxyExternalRequest: 'fetch',    // Pages 互換
      incrementalCache: 'dummy',
      tagCache: 'dummy',
      queue: 'dummy',
    },
  },
  middleware: {
    external: true,
    override: {
      wrapper: 'cloudflare-edge',      // Pages Edge 用
      converter: 'edge',
      proxyExternalRequest: 'fetch',
      incrementalCache: 'dummy',
      tagCache: 'dummy',
      queue: 'dummy',
    },
  },
};
```

**重要な発見**:
- `cloudflare-node` と `cloudflare-edge` ラッパーは Pages Functions 向け
- 出力ディレクトリ `.open-next` は Pages に最適化
- Workers への直接デプロイは想定されていない

### 4. 実際のユースケース分析

#### Pages が適している場合（このプロジェクト）:
✅ Next.js App Router アプリケーション
✅ 静的アセット + サーバーサイドレンダリング
✅ Git 統合による自動デプロイ
✅ プレビューデプロイが必要
✅ R2, KV などの Cloudflare サービス使用
✅ 25MB 以内のビルドサイズ

#### Workers が適している場合:
✅ カスタム API サーバー（Next.js なし）
✅ 低レベルの HTTP 制御が必要
✅ Durable Objects が必要
✅ Cron Triggers が必要
✅ 複数のマイクロサービスを統合
✅ 既存の Workers エコシステムとの統合

### 5. 70-80% Workers 使用率の真相

調査の結果、以下の理由が考えられます:

#### 理由 1: 統計の誤解
- **Pages も内部的には Workers を使用している**
- Pages Functions は自動的に Workers として実行される
- 統計上 "Workers" としてカウントされている可能性

#### 理由 2: 歴史的背景
```
2017年: Workers リリース
2021年: Pages リリース（静的サイトのみ）
2022年: Pages Functions リリース
2023年: Pages Next.js サポート強化
2025年: 現在（まだ Pages は新しい）
```

- 古いプロジェクトは Workers を使用
- 新規プロジェクトは Pages に移行中
- 移行期間中のため、Workers 使用率が高い

#### 理由 3: 異なるユースケース
- **Workers**: API 専用、マイクロサービス、低レベル制御
- **Pages**: フルスタック Web アプリケーション

Next.js 15 + App Router のプロジェクトに限定すれば、**Pages 使用率の方が高い**と推測されます。

---

## 🎯 移行可否判断

### 移行する理由が**ない**
1. ✅ 現在の Pages 設定で問題なく動作
2. ✅ @opennextjs/cloudflare は Pages に最適化
3. ✅ Git 統合とプレビューデプロイが有用
4. ✅ 環境変数とバインディングの管理が容易
5. ✅ CI/CD ワークフローが Pages 向けに最適化済み

### 移行する理由がある場合
1. ❌ より大きなスクリプトサイズが必要（現在 50MB → 制限 25MB）
   - **現状**: ビルドサイズ 50MB だが、実際の Functions は分割されているため問題なし
2. ❌ Durable Objects が必要
   - **現状**: KV と R2 で十分
3. ❌ Cron Triggers が必要
   - **現状**: 不要
4. ❌ より低レベルの HTTP 制御が必要
   - **現状**: Next.js の抽象化で十分

### 結論: **移行不要**

---

## 📊 移行する場合の手順（参考）

万が一、将来的に Workers への移行が必要になった場合の手順:

### Phase 1: 準備（2-3日）

#### 1.1 ビルド出力の分析
```bash
# 現在のビルド
npm run build
du -sh .open-next/

# Workers 形式への変換調査
# @opennextjs/cloudflare は Workers 直接サポートなし
# 代替: @cloudflare/next-on-pages を検討
```

#### 1.2 wrangler.toml の更新
```toml
# 現在（Pages 用）
name = "akyodex-nextjs"
compatibility_date = "2025-01-22"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".open-next"

# Workers 用に変更
name = "akyodex-nextjs-worker"
main = ".open-next/worker.js"  # または変換後のファイル
compatibility_date = "2025-01-22"
compatibility_flags = ["nodejs_compat"]

# バインディング（Pages Dashboard から移行）
[[kv_namespaces]]
binding = "AKYO_KV"
id = "YOUR_KV_NAMESPACE_ID"

[[r2_buckets]]
binding = "AKYO_BUCKET"
bucket_name = "akyo-images"

# 環境変数
[vars]
ADMIN_PASSWORD_HASH = "..."
OWNER_PASSWORD_HASH = "..."
JWT_SECRET = "..."
```

### Phase 2: アダプターの変更（3-5日）

#### 2.1 @cloudflare/next-on-pages への切り替え

```bash
# 現在の依存関係削除
npm uninstall @opennextjs/cloudflare

# 新しいアダプターインストール
npm install -D @cloudflare/next-on-pages
npm install @cloudflare/next-on-pages/next-dev
```

#### 2.2 ビルドスクリプト変更

```json
// package.json
{
  "scripts": {
    "dev": "next dev --turbopack",
    "next:build": "next build",
    "build": "@cloudflare/next-on-pages",  // 変更
    "preview": "wrangler pages dev .vercel/output/static"
  }
}
```

#### 2.3 Next.js 設定の更新

```typescript
// next.config.ts
import { setupDevPlatform } from '@cloudflare/next-on-pages/next-dev';

if (process.env.NODE_ENV === 'development') {
  await setupDevPlatform();
}

const nextConfig: NextConfig = {
  // 既存の設定...
};

export default nextConfig;
```

### Phase 3: デプロイ方法の変更（1-2日）

#### 3.1 GitHub Actions ワークフロー更新

```yaml
# .github/workflows/deploy-cloudflare-workers.yml（新規）
name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build for Cloudflare Workers
        run: npm run build
        env:
          ADMIN_PASSWORD_HASH: ${{ secrets.ADMIN_PASSWORD_HASH }}
          OWNER_PASSWORD_HASH: ${{ secrets.OWNER_PASSWORD_HASH }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
      
      - name: Deploy to Cloudflare Workers
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
```

#### 3.2 プレビューデプロイの実装

Workers にはプレビューデプロイ機能がないため、手動で実装:

```yaml
# PR ごとに一時的な Workers を作成
- name: Deploy preview
  run: |
    PREVIEW_NAME="akyodex-pr-${{ github.event.pull_request.number }}"
    npx wrangler deploy --name $PREVIEW_NAME
```

### Phase 4: 移行実行（1日）

#### 4.1 テスト環境での検証
1. 新しいビルドシステムでビルド
2. wrangler dev でローカルテスト
3. ステージング Workers へデプロイ
4. 全機能テスト

#### 4.2 本番移行
1. DNS 切り替え準備
2. Workers へデプロイ
3. DNS を Workers URL に変更
4. Pages プロジェクトの無効化（バックアップとして保持）

### 推定移行コスト

| フェーズ | 期間 | 工数 | リスク |
|---------|------|------|-------|
| Phase 1: 準備 | 2-3日 | 中 | 低 |
| Phase 2: アダプター変更 | 3-5日 | 高 | 中 |
| Phase 3: デプロイ変更 | 1-2日 | 中 | 中 |
| Phase 4: 移行実行 | 1日 | 低 | 高 |
| **合計** | **7-11日** | **高** | **中~高** |

---

## 🚨 移行時のリスクと注意点

### リスク 1: ビルド出力の互換性
**問題**: @opennextjs/cloudflare の出力が Workers に直接対応していない
**対策**: @cloudflare/next-on-pages への切り替えが必要（大規模な変更）

### リスク 2: プレビューデプロイの喪失
**問題**: Workers にはプレビューデプロイ機能がない
**対策**: 手動実装が必要（複雑なワークフロー）

### リスク 3: 環境変数管理の複雑化
**問題**: wrangler.toml にシークレットを含められない
**対策**: wrangler secret コマンドまたは CI/CD での設定が必要

### リスク 4: デプロイパイプラインの再構築
**問題**: 既存の CI/CD ワークフローが Pages 向けに最適化されている
**対策**: 全ワークフローの書き直しが必要

### リスク 5: ダウンタイム
**問題**: DNS 切り替え時にダウンタイムが発生する可能性
**対策**: 段階的な移行戦略が必要

---

## 📈 Next.js 16 移行への影響

### Next.js 16 への移行順序

**元の計画**: Workers → Next.js 16
**推奨される順序**: Next.js 16 → （必要に応じて）Workers

#### 理由:
1. Next.js 16 は Pages で問題なく動作
2. @opennextjs/cloudflare は Next.js 16 をサポート予定
3. Workers 移行は Next.js 16 でも同じ手順
4. リスクを段階的に分散

### Next.js 16 移行時の注意点

```typescript
// Next.js 16 では以下が変更される可能性:
// 1. Cache Components mode
// 2. Async Request APIs（既に対応済み）
// 3. 新しい設定オプション
```

**Pages での Next.js 16 サポート**:
- @opennextjs/cloudflare のアップデートで対応可能
- 大きな変更は不要（現在の設定を維持）

**Workers での Next.js 16 サポート**:
- @cloudflare/next-on-pages も同様にアップデート
- どちらでも Next.js 16 への移行パスは存在

---

## 💼 CI/CD ワークフローへの影響

### 現在のワークフロー（Pages 向け）

```
ci.yml:
- Lint & Type Check
- Build Validation (npm run build → .open-next)
- Security Scan
- Dependency Review
- Performance Report

deploy-cloudflare-pages.yml:
- Build (npm run build)
- Verify output (.open-next/)
- Deploy (wrangler pages deploy .open-next)
- Health check

security-audit.yml:
- Weekly security scans

validate-cloudflare-resources.yml:
- R2, KV, CSV validation

nextjs-health-check.yml:
- Next.js best practices validation
```

### Workers 移行時の変更が必要な箇所

#### 1. deploy-cloudflare-pages.yml → deploy-cloudflare-workers.yml

```yaml
# 変更前（Pages）
- name: Deploy to Cloudflare Pages
  uses: cloudflare/wrangler-action@v3
  with:
    command: pages deploy .open-next --project-name=akyodex-nextjs

# 変更後（Workers）
- name: Deploy to Cloudflare Workers
  run: npx wrangler deploy
  env:
    CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
```

#### 2. validate-cloudflare-resources.yml

```yaml
# Pages: Dashboard 設定を前提
# Workers: wrangler.toml の設定を検証

# 追加の検証が必要:
- name: Validate wrangler.toml bindings
  run: |
    # KV namespace ID が正しいか
    # R2 bucket name が正しいか
    npx wrangler kv:namespace list
    npx wrangler r2 bucket list
```

#### 3. プレビューデプロイの追加（新規）

```yaml
# preview-deploy.yml（新規作成）
name: Deploy Preview

on:
  pull_request:
    branches:
      - main

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: npm ci
      - run: npm run build
      - name: Deploy to preview worker
        run: |
          PREVIEW_NAME="akyodex-pr-${{ github.event.pull_request.number }}"
          npx wrangler deploy --name $PREVIEW_NAME
          echo "Preview URL: https://$PREVIEW_NAME.<subdomain>.workers.dev"
```

### ドキュメントの更新が必要な箇所

#### 更新が必要なドキュメント:

1. ✏️ **README.md**
   - デプロイ方法のセクション
   - Cloudflare Pages → Workers に変更

2. ✏️ **DEPLOYMENT.md**
   - 全体を Workers デプロイメント向けに書き直し
   - wrangler.toml の設定方法
   - シークレット管理の変更

3. ✏️ **.github/workflows/README.md**
   - デプロイワークフローの説明更新
   - プレビューデプロイの説明追加

4. ✏️ **NEXTDEVTOOLS-IMPROVEMENTS.md**
   - Workers への移行に関するセクション追加
   - Next.js 16 + Workers の組み合わせについて

5. ✏️ **QUICKSTART.md**
   - セットアップ手順の更新
   - wrangler secret コマンドの使用方法

---

## 🎓 推奨事項

### 短期（現在）
**✅ Cloudflare Pages のまま継続**

理由:
- 現在の設定で問題なく動作
- Git 統合とプレビューデプロイが有用
- 移行コストが利益を上回る
- リスクが高い

### 中期（Next.js 16 移行時）
**✅ Pages で Next.js 16 に移行**

手順:
1. @opennextjs/cloudflare を最新版に更新
2. Next.js 16 にアップグレード
3. 互換性テスト
4. デプロイ

### 長期（状況次第）
**🔄 Workers への移行を検討する条件**

以下のいずれかが該当する場合のみ:
1. ビルドサイズが 25MB を恒常的に超える
2. Durable Objects が必須になる
3. Cron Triggers が必須になる
4. Cloudflare が Pages の Next.js サポートを終了（可能性は低い）

---

## 📄 まとめ

### 調査結論
1. **70-80% Workers 使用率は誤解または特定のユースケース**
   - Pages も内部的には Workers を使用
   - Next.js 15 + App Router では Pages が適切

2. **現在の Pages 設定は最適**
   - @opennextjs/cloudflare は Pages に最適化
   - Git 統合、プレビューデプロイが有用
   - 環境変数とバインディング管理が容易

3. **Workers への移行は不要**
   - 明確なメリットなし
   - 高い移行コストとリスク
   - Pages で十分な機能を提供

4. **Next.js 16 への移行順序**
   - **推奨**: Next.js 16 → （必要に応じて）Workers
   - Pages で Next.js 16 への移行パスが明確

### アクション
- ✅ **現状維持**: Cloudflare Pages を継続使用
- ✅ **Next.js 16 移行**: Pages のまま Next.js 16 にアップグレード
- 📝 **ドキュメント追記**: この調査結果を記録
- 🔄 **定期的な再評価**: 6ヶ月ごとに状況を確認

---

**初版調査日**: 2025-11-07
**最終更新**: 2026-02-16 20:17:05 JST / 2026-02-16 11:17:05 UTC
**調査者**: GitHub Copilot, Codex
**対象プロジェクト**: Akyodex Next.js (Next.js 15.5.10 + App Router + Cloudflare Pages)
**現行結論**: **Cloudflare Pages での継続使用を推奨。Workers への移行は必要条件が揃うまで保留。**
