#!/bin/sh

node scripts/push-and-check-pr-conflicts.js --fail-if-merged
status=$?

if [ "$status" -ne 0 ]; then
  echo >&2 ""
  echo >&2 "pre-push hook blocked this push."
  exit "$status"
fi

exit 0
